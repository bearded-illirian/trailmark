# Block 1-3 — config loader + FastAPI backend
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# Блок 71030 задачи radar-os--695 — показ docx в портале клиента.
# Ставится РУКАМИ: пост-receive хук обновляет код и pip не зовёт, так что эта
# строка здесь документ, а не команда. Без установки работает запасной разбор
# на стандартной библиотеке, и в журнале сервиса об этом есть запись.
mammoth>=1.12
